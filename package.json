{
  "name": "enterprise-playwright",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test:smoke": "cucumber-js --tags @smoke --parallel 3 --retry 2 --format @cucumber/pretty-formatter --format allure-cucumberjs/reporter",
  "test:regression": "cucumber-js --tags @regression --parallel 3 --retry 2 --format @cucumber/pretty-formatter --format allure-cucumberjs/reporter",
  "test": "cucumber-js --parallel 3 --retry 2 --format @cucumber/pretty-formatter --format allure-cucumberjs/reporter"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@cucumber/cucumber": "^12.7.0",
    "@playwright/test": "^1.58.2",
    "@types/node": "^25.2.3",
    "allure-commandline": "^2.38.0",
    "allure-cucumberjs": "^3.6.0",
    "allure-playwright": "^3.6.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  }
}
